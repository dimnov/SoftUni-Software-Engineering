function cityRecord(cityName, population, treasury) {
  let city = {
    name: cityName,
    population: population,
    treasury: treasury,
  }
  return city;
}
cityRecord('Tortuga',
  7000,
  15000);